@*@model NIMBOLE.Models.Models.ProductNamesModel*@
@model int?
@Html.TextBox("Comp2Name", new { @placeholder = @NIMBOLE.GlobalResources.Resources.Value, @class = "form-control", @readonly = "readonly" })
@*@(Html.Kendo().ComboBox()
    .Name("Comp2Name")
    .DataTextField("ProductName")
    .DataValueField("ProductNamesId")
    .Filter(FilterType.StartsWith)
    .AutoBind(true)
    .ValuePrimitive(true)
    .Placeholder(@NIMBOLE.GlobalResources.Resources.SelectOrAdd)
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetProductNames", "Products");
        }).Events(e=>e.RequestEnd("onComp2ProductRequestEnd"))

        .ServerFiltering(true);
    })
        .Events(e => e.Change("onChangeComp2"))
    .Suggest(true)
)*@
